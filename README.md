# Books-Project-FastAPI-Usage
Used the fundamentals of FastAPI with CRUD and HTPP request methods to build a library of books where user can POST book GET book PUT book and DELETE book along with some additional query functions for further usability. Version 2 now uploaded which adds Pydantic validation for user entries and now uses classes and contructors for book paramaters and entries. 
